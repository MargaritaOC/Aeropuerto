<?php
    include_once '../Conecta/conexion.php';
    $numVuelo = 1;
    $conexion = Conexion::obtenerConexion();

    // Consulta
  //  $query = "SELECT * FROM pasajero";
  //  $sentencia = $conexion->prepare($query); // Llamar a la query

    //$salida = $sentencia->execute($query);

   // echo $salida;

?>